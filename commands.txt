#dev
docker-compose up -d

#prod
 docker-compose -f docker-compose.yml -f docker-compose.prod.yml  up -d

# Generate a 2048 bit DH Param file
sudo openssl dhparam -out dhparam-2048.pem 2048

docker run 

# list containers
docker ps -a

docker kill 

docker rm - f

docker build dockerfile

docker image 

----

how to make lean containter
progressive builds
